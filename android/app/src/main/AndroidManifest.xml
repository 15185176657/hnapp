<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="hnapp"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
              <!-- Android 进程启动后立即应用到 Activity 的主题。
                  Flutter UI 初始化前用户会看到该主题；初始化后它仍决定 Flutter UI 背后的窗口背景。 -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 不要删除下面的 meta-data，Flutter 工具会用它生成 GeneratedPluginRegistrant.java。 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
        <!-- 查询可处理文本的 Activity 所需配置，参考：
         https://developer.android.com/training/package-visibility 以及
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

            Flutter 引擎中的 io.flutter.plugin.text.ProcessTextPlugin 会使用它。 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
